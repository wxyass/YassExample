<resources>

    <!--缓冲页面-->
    <style name="loading_dialog" parent="@android:style/Theme.Dialog">
        <!--去掉边框-->
        <item name="android:windowFrame">@null</item>
        <!--悬浮-->
        <item name="android:windowIsFloating">true</item>
        <!--半透明-->
        <item name="android:windowIsTranslucent">false</item>
        <!--不需要标题-->
        <item name="android:windowNoTitle">true</item>
        <!--背景透明-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--允许模糊-->
        <item name="android:backgroundDimEnabled">true</item>
        <!--全屏幕-->
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- 多选弹窗上标题中的文字 -->
    <style name="dialog_text_title" >
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">#ffffff</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- 筛选终端 产品列表文字 -->
    <style name="checkterm_proname" >
        <item name="android:textSize">20dp</item>
        <item name="android:textColor">#555555</item>
        <item name="android:gravity">left|center_vertical</item>
    </style>

    <!-- ListView上每个item的背景色 -->
    <style name="lv_bg_item">
        <item name="android:background">@color/bg_lv_item</item>
        <item name="android:minHeight">50dp</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <style name="AnimationTopFade">
        <item name="android:windowEnterAnimation">@anim/dropdown_in</item>
        <item name="android:windowExitAnimation">@anim/dropdown_out</item>
    </style>

    <!-- ListView上每个item中的文字(最小) -->
    <style name="listview_tv_item_text_xiao" parent="@style/font_size7">
        <item name="android:textColor">#666464</item>
        <item name="android:gravity">center_vertical|left</item>
        <item name="android:paddingLeft">5dp</item>
    </style>

    <!-- ListView上每个item中的文字(最最小) -->
    <style name="listview_tv_item_text_xiao2" parent="@style/font_size8">
        <item name="android:textColor">#666464</item>
        <item name="android:gravity">center_vertical|left</item>
        <item name="android:paddingLeft">5dp</item>
    </style>

</resources>
